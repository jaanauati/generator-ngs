default:
  - 'clean'<% if (context.jsPre.toLowerCase() == "coffeescript") { %>
  - 'coffeelint'
  - 'coffee:compile'<% } %><% if (context.cssPre.toLowerCase() == "compass") { %>
  - 'compass:build'<% } %>
  - 'uglify'
  - 'clean'
